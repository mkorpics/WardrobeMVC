@model IEnumerable<WardrobeMVCProject.Models.ArticleType>

@{
    ViewBag.Title = "Index";
}

<div class="page-header textBox container">
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn superheroBtn", role = "button" })
    </p>
</div>

<div class="alternateTextBox">
    <table class="table">
        <tr>
            <th class="bold">
                @Html.DisplayNameFor(model => model.ArticleType1)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ArticleType1)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ArticleTypeID }, new { @class = "btn superheroBtn", role = "button" })
                    @Html.ActionLink("Details", "Details", new { id = item.ArticleTypeID }, new { @class = "btn superheroBtn", role = "button" })
                    @Html.ActionLink("Delete", "Delete", new { id = item.ArticleTypeID }, new { @class = "btn superheroBtn ", role = "button" })
                </td>
            </tr>
        }

    </table>
</div>
