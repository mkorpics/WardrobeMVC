@model WardrobeMVCProject.Models.Season

@{
    ViewBag.Title = "Delete";
}

<div class="page-header textBox container">
    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
</div>

<div class="alternateTextBox">
    <h4>Delete Season</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Season1)
        </dt>

        <dd class="textLeft">
            @Html.DisplayFor(model => model.Season1)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn superheroBtn" /> |
            @Html.ActionLink("Back to Seasons", "Index", new { }, new { @class = "btn superheroBtn", role = "button" })
        </div>
    }
</div>
